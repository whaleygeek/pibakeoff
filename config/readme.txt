interfaces.static configures a static IP address (two Pi's plugged together)

sudo cp interfaces.static /etc/network/interfaces
sudo reboot



interfaces.dhcp configures a dynamic dhcp address (using a router)

sudo cp interfaces.dhcp /etc/network/interfaces
sudo reboot


